# Repository workflow

- Work on a dedicated Git branch for every change; never modify or commit directly on `main`.
- Before editing, check the current branch and create or switch to an appropriate branch if needed.
- Commit changes to the working branch and push that branch, unless the user explicitly requests another workflow.
